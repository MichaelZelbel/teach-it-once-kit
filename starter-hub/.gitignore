# Keep secrets and machine junk out of your folder's history.
.env
.env.local
.DS_Store
node_modules/
__pycache__/
*.log

# Dev projects — each is its own GitHub repo, excluded from the hub repo.
# The folder + README stay tracked so the convention persists; contents don't.
dev/*
!dev/README.md
