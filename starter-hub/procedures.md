# Procedures: everything that runs on its own

<!-- Chapter 20's register, as the live file. One block per procedure,
     six lines. The rule: never run a procedure you cannot see and stop.
     No procedure exists unless it has a block here. -->

## (Procedure name)

Does: (what it does, in your own words).
Rhythm: (when it runs).            Lands: (where the result waits).
Lives: (which tool runs it).       Off-switch: (how to stop it).
Last checked: (date).
